# Copia este archivo como .env y rellena los valores

# Railway PostgreSQL (Railway lo genera automáticamente)
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# Secreto para JWT (genera uno aleatorio, ej: openssl rand -hex 32)
JWT_SECRET="cambia-este-secreto-por-uno-seguro-de-32-chars"
